const matchVolunteer = (volunteerId, eventId) => {
    // Implement logic to match volunteer to event
    return { message: 'Volunteer matched successfully' };
  };
  
  export default {
    matchVolunteer,
  };
  